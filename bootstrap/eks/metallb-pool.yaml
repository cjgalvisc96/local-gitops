apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: floci-eks
  namespace: metallb-system
spec:
  addresses:
    - __EKS_LB_IP__/32
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: floci-eks
  namespace: metallb-system
spec:
  ipAddressPools:
    - floci-eks
