apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app1

resources:
  - ../../../base
  - ingress.yaml

commonLabels:
  gitops.local/env: dev
  gitops.local/app: app1

# Environment-specific configuration (the GitOps demo: change this, Argo syncs it).
configMapGenerator:
  - name: app-config
    behavior: merge
    literals:
      - PODINFO_UI_COLOR=#2e7d32
      - PODINFO_UI_MESSAGE=app1 @ DEV
