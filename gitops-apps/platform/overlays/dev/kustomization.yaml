apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

commonLabels:
  gitops.local/env: dev

patches:
  - target:
      kind: ConfigMap
      name: platform-config
    patch: |
      - op: replace
        path: /data/ENVIRONMENT
        value: dev
